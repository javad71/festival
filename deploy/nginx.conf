server {
    listen 80;
    server_name festival.example.com www.festival.example.com;
    client_max_body_size 55m;

    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# After obtaining a certificate, replace the HTTP server with this HTTPS server:
# server {
#   listen 443 ssl http2;
#   server_name festival.example.com www.festival.example.com;
#   ssl_certificate /etc/letsencrypt/live/festival.example.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/festival.example.com/privkey.pem;
#   client_max_body_size 55m;
#   location / { proxy_pass http://app:8000; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto https; }
# }
